export default function (name: string) {
    // This is just a warm-up exercise, 
    // you just need to uncomment the code to pass the test.

    // use the DOM api to create an HTML `DIV`
    // const div = document.createElement('div');

    // use the `innerHTML` to add the text `Hello XXX!` into the `DIV`.
    // div.innerHTML = `Hello ${name}!`;

    //. add the `DIV` into the first element with CSS class `playground`.
    const playground = document.querySelector('.playground');
    // playground.appendChild(div);
}